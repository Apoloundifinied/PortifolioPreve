# ADMIN SECURITY CONFIGURATION
# ⚠️ IMPORTANTE: Configure estas variáveis antes de fazer deploy em produção!

# Senha do painel admin (mudará para hash em produção)
# MUDE ESTA SENHA IMEDIATAMENTE!
VITE_ADMIN_PASSWORD=admin123

# Caminho dinâmico para a rota admin (oculta a rota real)
# Exemplo: dashboard-abc123, admin-config, panel-settings
# Use um valor único e aleatório
VITE_ADMIN_PATH=dashboard-settings

# Chave secreta para hash de senha (mínimo 32 caracteres em produção)
VITE_ADMIN_SECRET=sua-chave-secreta-muito-longa-aqui-12345678

# SUPABASE (Opcional para autenticação em produção)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
